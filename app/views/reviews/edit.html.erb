<%= render partial: 'form', locals: {
                                      path: "/reviews/#{@review.id}",
                                      method: :patch,
                                      button_text: 'Update Review',
                                      review_header: "Edit Review",
                                      prefill_title: @review.title,
                                      prefill_rating: @review.rating,
                                      prefill_content: @review.content,
                                      prefill_image: @review.image,
                                      prefill_name: @review.name
                                    } %>
